<Project Sdk="Microsoft.NET.Sdk.Web">

  <!--
    The review UI (#75, ADR 0004): Blazor Server, interactive throughout.

    No PropertyGroup of its own. TargetFramework, Nullable, ImplicitUsings and
    TreatWarningsAsErrors come from src/Directory.Build.props, as they do for the Api and
    the Worker - repeating them here would be a second place to change and the first
    place to drift.

    No package references either, deliberately. Everything this needs ships with the
    framework, so there is nothing to upgrade, audit or lose track of.
  -->

</Project>

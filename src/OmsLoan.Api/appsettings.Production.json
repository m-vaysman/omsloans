{
  "//": "Production overrides. Contains no secrets and no listening address: the connection string arrives as an environment variable set on the service, and ASPNETCORE_URLS is set there too (see scripts/Install-OmsLoanApiService.ps1 and docs/api-windows-service.md). Both outrank this file.",

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.Hosting.Lifetime": "Information",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    },
    "EventLog": {
      "LogLevel": {
        "Default": "Warning",
        "OmsLoan.Api.Startup": "Information",
        "Microsoft.Hosting.Lifetime": "Information"
      }
    }
  }
}

<UserControl x:Class="OMS.Loans.Views.BlotterEntriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OMS.Loans.Views"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel MaxHeight="700" MaxWidth="1100">
        <dx:SimpleButton DockPanel.Dock="Top" HorizontalAlignment="Right" Glyph="{dx:DXImage GrayScaleImages/Actions/Reset_32x32.png}" Command="{Binding LoadBlotterEntriesCommand}"/>
        <dxg:GridControl LostFocus="GridControl_LostFocus" DockPanel.Dock="Bottom" ItemsSource="{Binding BlotterEntries}" SelectionMode="Row" SelectedItem="{Binding SelectedBlotterEntry}"  AutoGenerateColumns="None" >
            <dxg:GridControl.Columns>
                <dxg:GridColumn FieldName="BlotterId" Header="Id"/>
                <dxg:GridColumn FieldName="TradeAcct" />
                <dxg:GridColumn FieldName="TradeDate" />
                <dxg:GridColumn FieldName="CounterParty.CounterPartyName" />
                <dxg:GridColumn FieldName="Ticker" />
                <dxg:GridColumn FieldName="Price" />
                <dxg:GridColumn FieldName="Notional" />
                <dxg:GridColumn FieldName="Spread" />
            </dxg:GridControl.Columns>
            <dxg:GridControl.View>
                <dxg:TableView AllowHeaderNavigation="True" AllowEditing="False"  ShowCriteriaInAutoFilterRow="False" ShowSearchPanelMode="Always"/>
            </dxg:GridControl.View>
        </dxg:GridControl>
    </DockPanel>
</UserControl>
